import pytest


def processing_test():
     """This test checks the ouput of the scraper function
     """
     assert type(read_pandas) == pd.core.frame.DataFrame
     assert type(pandas_data_cleaning == pd.core.frame.DataFrame
     assert type(pandas_aggregate) == pd.core.frame.DataFrame
     assert type(pandas_agg) == pd.core.frame.DataFrame
